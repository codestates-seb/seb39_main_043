import styled from 'styled-components';

const JoinButtonWrapper = styled.div`
  width: 442px;
  height: 60px;
  border: 1px solid black;
`;

const JoinButton = () => {
  return <JoinButtonWrapper>회원가입</JoinButtonWrapper>;
};

export default JoinButton;
