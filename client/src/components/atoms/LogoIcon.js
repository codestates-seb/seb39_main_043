import styled from 'styled-components';

const LogoIconWrapper = styled.div`
  width: 80px;
  height: 40px;
  text-align: center;
  font-size: 14px;
`;

const LogoIcon = () => {
  return <LogoIconWrapper>Social Calendar</LogoIconWrapper>;
};

export default LogoIcon;
