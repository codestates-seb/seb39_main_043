import styled from 'styled-components';

const NavigationBarWrapper = styled.div`
  width: 1280px;
  height: 50px;
  background-color: #f5f5f5;
`;

const NavigationBar = () => {
  return <NavigationBarWrapper></NavigationBarWrapper>;
};

export default NavigationBar;
