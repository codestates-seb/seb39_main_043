import { IoIosArrowForward } from 'react-icons/io';

const NextMonthIcon = ({ className, onClick }) => {
  return <IoIosArrowForward size={24} className={className} onClick={onClick} />;
};

export default NextMonthIcon;
