import styled from "styled-components";

const Container = styled.span`
  margin-left: 8px;
`;

const ScheduleAttendee = () => {
  return <Container>참석자</Container>;
};

export default ScheduleAttendee;
