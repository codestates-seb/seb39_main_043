import styled from "styled-components";

const Container = styled.span`
  margin-left: 8px;
`;

const ScheduleExplain = () => {
  return <Container>설명</Container>;
};

export default ScheduleExplain;
