import styled from "styled-components";

const Container = styled.span`
  margin-left: 8px;
`;
const ScheduleDateTime = () => {
  return <Container>일시</Container>;
};

export default ScheduleDateTime;
